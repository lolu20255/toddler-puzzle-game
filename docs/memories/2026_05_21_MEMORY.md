# Session Memory — 2026-05-21

## Project snapshot
- **Toddlers Learning Puzzle** — a game for toddlers to learn shapes and colors.
- Stack: **Phaser 3.85** + **Vue 3.4** + **Vite 5**. `npm run dev` serves on `http://localhost:8080`.
- Vue ↔ Phaser bridge: `src/game/PhaserGame.vue` + `src/game/EventBus.js`.
- Scenes (`src/game/scenes/`): `Boot` → `MainMenu` → `GameA` / `GameB` / `GameC` → `GameOver`.
  - `GameA.js` and `GameB.js` are the largest scenes (~452 lines each); `GameC.js` ~351.
- Assets in `public/assets/` (animal-pack, backgrounds, buttons, clouds, emojis-pack, elements, etc.).

## Repo state at session start
- Active branch: `main-menu` (other branches: `main`, `multiple-shadow-rows`, `puzzle-b`, `puzzle-one`, `puzzle-text`).
- Uncommitted: `package-lock.json` modified; new untracked asset `public/assets/emojis-pack/603f1b4d-...png`.
- Recent commits centered on main-menu / game scene tweaks ("no label", "game random", "fixmenu", "new set").

## Work done this session
- Established a project-local **session memory system**:
  - Memory files live in `docs/memories/` named `YYYY_MM_DD_MEMORY.md` (this file is the first).
  - Created `CLAUDE.md` instructing Claude to read all files in `docs/memories/` at the start of every new session.
- **Redesigned the Main Menu** (`src/game/scenes/MainMenu.js`, full rewrite) for toddler usability:
  - Replaced plain "GAME A/B/C" text buttons with three big picture cards — **Toys** (`GameA`), **Heroes** (`GameB`), **Faces** (`GameC`) — each showing a representative sprite (teddy / elf / laughing face) so non-readers can choose.
  - New cheerful palette: gradient sky-blue background + grass hills, smiley sun, drifting clouds, twinkles. Cards are orange / purple / pink with 3D lip, drop shadow, white icon "window".
  - Adaptive layout: 3 cards in a row (landscape) or stacked (portrait).
  - Toddler UX: large touch targets, press squash-and-pop, star burst, `sfx_collect` sound, idle bob, fade transition; respects `prefers-reduced-motion`.
  - Game→pack mapping (note the misleading pack names): GameA `animal_cartoon` = **toys**, GameB `mistic_lego` = **fantasy heroes**, GameC `emojis_lego` = **emoji faces**.
  - Verified in Chromium (portrait + landscape) — renders clean, card tap navigates correctly, no console errors.

## Notes for next session
- No CLAUDE.md existed before today — it was created this session.
- Add a new dated memory file here at the end of each working session.
- Known pre-existing bug (NOT fixed — out of scope): in `GameA`, the back button overlaps the "SCORE" text in the top-left corner.
